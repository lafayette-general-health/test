---
  - name: BACKUP PLAYBOOK
    hosts: "{{ hostname }}"
    network_os: ios
    connection: network_cli
    gather_facts: yes

    tasks:
      - name: Backup current switch config (ios)
        ios_config:
          backup: yes
